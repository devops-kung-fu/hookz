# PURPOSE: Adds any changed files to the commit. Useful if the action pipeline changed anything
# HOOK: pre-push 
        - name: "git: Add all changed files during the pre-commit stage"
          exec: git
          args: ["add", "."]